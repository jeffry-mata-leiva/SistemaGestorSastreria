import 'package:flutter/material.dart';

class Lista_Ordenes extends StatelessWidget {
  const Lista_Ordenes({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Lista de Materiales Pendientes')),
    );
  }
}
